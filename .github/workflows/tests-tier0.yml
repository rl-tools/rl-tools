# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: Tests Tier 0

on:
  workflow_dispatch:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [ubuntu-24.04, ubuntu-22.04, windows-latest, macos-14]
        build_type: [Release]
        c_compiler: [gcc, clang, cl]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
          - os: ubuntu-24.04
            c_compiler: gcc
            cpp_compiler: g++
          - os: ubuntu-24.04
            c_compiler: clang
            cpp_compiler: clang++
          - os: ubuntu-22.04
            c_compiler: gcc
            cpp_compiler: g++
          - os: ubuntu-22.04
            c_compiler: clang
            cpp_compiler: clang++
          - os: macos-14
            c_compiler: clang
            cpp_compiler: clang++
        exclude:
          - os: windows-latest
            c_compiler: gcc
          - os: windows-latest
            c_compiler: clang
          - os: ubuntu-24.04
            c_compiler: cl
          - os: ubuntu-22.04
            c_compiler: cl
          - os: macos-14
            c_compiler: cl
          - os: macos-14
            c_compiler: gcc

    steps:
      - uses: actions/checkout@v3

      - name: Set reusable strings
        # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
        id: strings
        shell: bash
        run: |
          echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

      - name: Download Test Data
        shell: bash
        run: ./tests/download_data.sh
      - name: Configure CMake
        # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
        # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
        run: >
          cmake -B ${{ steps.strings.outputs.build-output-dir }}
          -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
          -DCMAKE_C_COMPILER=${{ matrix.c_compiler }}
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
          -DRL_TOOLS_ENABLE_TESTS=ON
          -S ${{ github.workspace }}

      - name: Build
        # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
        run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

      - name: Test
        working-directory: ${{ steps.strings.outputs.build-output-dir }}
        # Execute tests defined by the CMake configuration. Note that --build-config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
        # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
        run: ctest --build-config ${{ matrix.build_type }} --output-on-failure

  raytracing-generic:
    name: Raytracing Generic
    runs-on: ubuntu-24.04
    env:
      CMAKE_BUILD_PARALLEL_LEVEL: "5"
      CTEST_PARALLEL_LEVEL: "5"

    steps:
      - uses: actions/checkout@v3

      - name: Install raytracing dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libassimp-dev cmake

      - name: Configure CMake
        run: >
          cmake -S . -B build-raytracing-generic
          -DCMAKE_BUILD_TYPE=Release
          -DRL_TOOLS_ENABLE_TARGETS=ON
          -DRL_TOOLS_ENABLE_TESTS=ON
          -DRL_TOOLS_BACKEND_DISABLE_CUDA=ON
          -DRL_TOOLS_RENDERING_ENABLE_RAYTRACING=ON
          -DRL_TOOLS_RENDERING_RAYTRACING_BACKEND=GENERIC

      - name: Build overlay scenarios
        run: cmake --build build-raytracing-generic --target test_rendering_raytracing_overlay_scenarios_generic --parallel 5

      - name: Test overlay scenarios
        working-directory: build-raytracing-generic
        run: ctest -L overlay --no-tests=error --output-on-failure --timeout 20 -j5

  raytracing-vulkan:
    name: Raytracing Vulkan
    runs-on: ubuntu-24.04
    env:
      CMAKE_BUILD_PARALLEL_LEVEL: "5"
      CTEST_PARALLEL_LEVEL: "5"

    steps:
      - uses: actions/checkout@v3

      - name: Install raytracing dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libassimp-dev libvulkan-dev glslang-tools mesa-vulkan-drivers cmake

      - name: Select Lavapipe driver
        run: |
          lavapipe_manifest="$(find /usr/share/vulkan/icd.d -maxdepth 1 -name 'lvp_icd*.json' -print -quit)"
          test -n "$lavapipe_manifest"
          echo "VK_DRIVER_FILES=$lavapipe_manifest" >> "$GITHUB_ENV"

      - name: Configure CMake
        run: >
          cmake -S . -B build-raytracing-vulkan
          -DCMAKE_BUILD_TYPE=Release
          -DRL_TOOLS_ENABLE_TARGETS=ON
          -DRL_TOOLS_ENABLE_TESTS=ON
          -DRL_TOOLS_BACKEND_DISABLE_CUDA=ON
          -DRL_TOOLS_RENDERING_ENABLE_RAYTRACING=ON
          -DRL_TOOLS_RENDERING_RAYTRACING_BACKEND=VULKAN

      - name: Build overlay scenarios
        run: cmake --build build-raytracing-vulkan --target test_rendering_raytracing_overlay_scenarios_vulkan --parallel 5

      - name: Test overlay scenarios
        working-directory: build-raytracing-vulkan
        run: ctest -L overlay --no-tests=error --output-on-failure --timeout 20 -j5
