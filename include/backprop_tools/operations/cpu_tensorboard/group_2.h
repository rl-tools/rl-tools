#include "../../version.h"
#if (defined(BACKPROP_TOOLS_DISABLE_INCLUDE_GUARDS) || !defined(BACKPROP_TOOLS_OPERATIONS_CPU_TENSORBOARD_GROUP_2_H)) && (BACKPROP_TOOLS_USE_THIS_VERSION == 1)
#pragma once
#define BACKPROP_TOOLS_OPERATIONS_CPU_TENSORBOARD_GROUP_2_H
#if defined(BACKPROP_TOOLS_DISABLE_INCLUDE_GUARDS) || !defined(BACKPROP_TOOLS_OPERATIONS_CPU_TENSORBOARD_GROUP_2)
    #define BACKPROP_TOOLS_OPERATIONS_CPU_TENSORBOARD_GROUP_2
    #include "../../utils/assert/operations_cpu.h"
#else
    #error "Group 2 already imported"
#endif
#endif
